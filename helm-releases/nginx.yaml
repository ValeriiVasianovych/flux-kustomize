apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nginx
  namespace: nginx
spec:
  interval: 5m
  releaseName: nginx
  chart:
    spec:
      chart: nginx
      version: "4.7.1"
      sourceRef:
        kind: HelmRepository
        name: nginx
        namespace: flux-system